## Map cached edges with links metrics and contact info documentation

* for elem in links_metrics
  * if not cached_edge
    ```
    self._logger.info(f"No cached info was found for edge {serial_number}. Skipping...")
    ```
  