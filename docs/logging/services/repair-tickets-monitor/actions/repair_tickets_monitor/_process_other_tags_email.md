## Process other tags email

```python
log.info(f"Marking email {email.id} as complete because it's tagged as '{tag_name}'")
```

[NewTaggedEmailsRepository:mark_complete](../../repositories/new_tagged_emails_repository/mark_complete.md)
