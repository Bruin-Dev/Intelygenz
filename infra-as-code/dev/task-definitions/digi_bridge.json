[
  {
    "name": "digi-bridge",
    "image": "${image}",
    "portMappings": [
      {
        "containerPort": 5000,
        "hostPort": 5000
      },
      {
        "containerPort": 9090,
        "hostPort": 9090
      }
    ],
    "networkMode": "awsvpc",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${log_group}",
        "awslogs-region": "us-east-1",
        "awslogs-stream-prefix": "${log_prefix}"
      }
    },
    "healthCheck": {
      "command": [
        "CMD-SHELL",
        "curl -f http://localhost:5000/_health || exit 1"
      ],
      "interval": 30,
      "retries": 3,
      "startPeriod": 30,
      "timeout": 5
    },
    "environment": [
      {
        "name": "PYTHONUNBUFFERED",
        "value": "${PYTHONUNBUFFERED}"
      },
      {
        "name": "NATS_SERVER1",
        "value": "${NATS_SERVER1}"
      },
      {
        "name": "REDIS_HOSTNAME",
        "value": "${REDIS_HOSTNAME}"
      },
      {
        "name": "DIGI_CLIENT_ID",
        "value": "${DIGI_CLIENT_ID}"
      },
      {
        "name": "DIGI_CLIENT_SECRET",
        "value": "${DIGI_CLIENT_SECRET}"
      },
      {
        "name": "DIGI_BASE_URL",
        "value": "${DIGI_BASE_URL}"
      },
      {
        "name": "DIGI_IP_PRO",
        "value": "${DIGI_IP_PRO}"
      },
      {
        "name": "DIGI_RECORD_NAME_PRO",
        "value": "${DIGI_RECORD_NAME_PRO}"
      },
      {
        "name": "DIGI_IP_DEV",
        "value": "${DIGI_IP_DEV}"
      },
      {
        "name": "DIGI_RECORD_NAME_DEV",
        "value": "${DIGI_RECORD_NAME_DEV}"
      },
      {
        "name": "DIGI_IP_TEST",
        "value": "${DIGI_IP_TEST}"
      },
      {
        "name": "DIGI_RECORD_NAME_TEST",
        "value": "${DIGI_RECORD_NAME_TEST}"
      },
      {
        "name": "CURRENT_ENVIRONMENT",
        "value": "${CURRENT_ENVIRONMENT}"
      },
      {
        "name": "PAPERTRAIL_ACTIVE",
        "value": "${PAPERTRAIL_ACTIVE}"
      },
      {
        "name": "PAPERTRAIL_HOST",
        "value": "${PAPERTRAIL_HOST}"
      },
      {
        "name": "PAPERTRAIL_PORT",
        "value": "${PAPERTRAIL_PORT}"
      },
      {
        "name": "PAPERTRAIL_PREFIX",
        "value": "${PAPERTRAIL_PREFIX}"
      },
      {
        "name": "ENVIRONMENT_NAME",
        "value": "${ENVIRONMENT_NAME}"
      }
    ]
  }
]