[
  {
    "name": "${container_name}",
    "image": "${image}",
    "portMappings": [
      {
        "containerPort": 5000,
        "hostPort": 5000
      },
      {
        "containerPort": 9090,
        "hostPort": 9090
      }
    ],
    "networkMode": "awsvpc",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${log_group}",
        "awslogs-region": "us-east-1",
        "awslogs-stream-prefix": "${log_prefix}"
      }
    },
    "healthCheck": {
      "command": [
        "CMD-SHELL",
        "curl -f http://localhost:5000/_health || exit 1"
      ],
      "interval": 30,
      "retries": 3,
      "startPeriod": 30,
      "timeout": 5
    },
    "environment": [
      {
        "name": "PYTHONUNBUFFERED",
        "value": "${PYTHONUNBUFFERED}"
      },
      {
        "name": "NATS_SERVER1",
        "value": "${NATS_SERVER1}"
      },
      {
        "name": "CURRENT_ENVIRONMENT",
        "value": "${CURRENT_ENVIRONMENT}"
      },
      {
        "name": "LAST_CONTACT_RECIPIENT",
        "value": "${LAST_CONTACT_RECIPIENT}"
      },
      {
        "name": "REDIS_HOSTNAME",
        "value": "${REDIS_HOSTNAME}"
      },
      {
        "name": "VELOCLOUD_HOSTS",
        "value": "${VELOCLOUD_HOSTS}"
      },
      {
        "name": "VELOCLOUD_HOSTS_FILTER",
        "value": "${VELOCLOUD_HOSTS_FILTER}"
      },
      {
        "name": "ENABLE_TRIAGE_MONITORING",
        "value": "${ENABLE_TRIAGE_MONITORING}"
      }
    ]
  }
]
