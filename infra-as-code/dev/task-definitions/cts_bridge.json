[
  {
    "name": "cts-bridge",
    "image": "${image}",
    "portMappings": [
      {
        "containerPort": 5000,
        "hostPort": 5000
      },
      {
        "containerPort": 9090,
        "hostPort": 9090
      }
    ],
    "networkMode": "awsvpc",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${log_group}",
        "awslogs-region": "us-east-1",
        "awslogs-stream-prefix": "${log_prefix}"
      }
    },
    "healthCheck": {
      "command": [
        "CMD-SHELL",
        "curl -f http://localhost:5000/_health || exit 1"
      ],
      "interval": 30,
      "retries": 3,
      "startPeriod": 30,
      "timeout": 5
    },
    "environment": [
      {
        "name": "PYTHONUNBUFFERED",
        "value": "${PYTHONUNBUFFERED}"
      },
      {
        "name": "NATS_SERVER1",
        "value": "${NATS_SERVER1}"
      },
      {
        "name": "REDIS_HOSTNAME",
        "value": "${REDIS_HOSTNAME}"
      },
      {
        "name": "CTS_CLIENT_ID",
        "value": "${CTS_CLIENT_ID}"
      },
      {
        "name": "CTS_CLIENT_SECRET",
        "value": "${CTS_CLIENT_SECRET}"
      },
      {
        "name": "CTS_CLIENT_USERNAME",
        "value": "${CTS_CLIENT_USERNAME}"
      },
      {
        "name": "CTS_CLIENT_PASSWORD",
        "value": "${CTS_CLIENT_PASSWORD}"
      },
      {
        "name": "CTS_CLIENT_SECURITY_TOKEN",
        "value": "${CTS_CLIENT_SECURITY_TOKEN}"
      },
      {
        "name": "CTS_LOGIN_URL",
        "value": "${CTS_LOGIN_URL}"
      },
      {
        "name": "CTS_DOMAIN",
        "value": "${CTS_DOMAIN}"
      },
      {
        "name": "CURRENT_ENVIRONMENT",
        "value": "${CURRENT_ENVIRONMENT}"
      },
      {
        "name": "PAPERTRAIL_ACTIVE",
        "value": "${PAPERTRAIL_ACTIVE}"
      },
      {
        "name": "PAPERTRAIL_HOST",
        "value": "${PAPERTRAIL_HOST}"
      },
      {
        "name": "PAPERTRAIL_PORT",
        "value": "${PAPERTRAIL_PORT}"
      },
      {
        "name": "PAPERTRAIL_PREFIX",
        "value": "${PAPERTRAIL_PREFIX}"
      },
      {
        "name": "ENVIRONMENT_NAME",
        "value": "${ENVIRONMENT_NAME}"
      }
    ]
  }
]