# ==============================================
# Linter validation jobs for dispatch-portal module
# ==============================================
dispatch-portal-linter:
  variables:
    MODULE: dispatch-portal
  extends: .lint_module

# =======================================
# Unit tests jobs for dispatch-portal module
# =======================================
dispatch-portal-test:
  variables:
    MODULE: dispatch-portal
  extends: .unittest_module_python_alpine

# ==========================================
# Images builds jobs for dispatch-portal module
# ==========================================
dispatch-portal-build:
  stage: build
  variables:
    MODULE_NAME: dispatch-portal
    MODULE_VERSION: ${CI_PIPELINE_ID}
  extends: .build_app_image
