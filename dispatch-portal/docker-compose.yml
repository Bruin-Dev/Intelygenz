version: '3.6'
services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
  nginx:
    build:
      context: ./nginx
    ports:
      - 80:80
    depends_on:
      - nextjs
    network_mode: "host"