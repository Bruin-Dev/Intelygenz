# ==========================================================
# Linter validation jobs for repair-ticket-kre-bridge module
# ==========================================================
repair-ticket-kre-bridge-linter:
  variables:
    MODULE: repair-ticket-kre-bridge
  rules:
    - if: '($CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH =~ /^(feature|fix)\/.+$/ || $CI_COMMIT_BRANCH =~ /^dev\/(feature|fix)\/.+$/) && $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TITLE =~ /(feat|fix|perf|refactor)\(.*\): .*$/'
      changes:
        - repair-ticket-kre-bridge/src/application/**/*
        - repair-ticket-kre-bridge/src/config/**/*
        - repair-ticket-kre-bridge/src/app.py
        - repair-ticket-kre-bridge/Dockerfile
        - repair-ticket-kre-bridge/requirements.txt
    - if: '($CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH =~ /^(feature|fix)\/.+$/ || $CI_COMMIT_BRANCH =~ /^dev\/(feature|fix)\/.+$/) && $CI_PIPELINE_SOURCE == "web"'
  extends: .lint_module

# ===================================================
# Unit tests jobs for repair-ticket-kre-bridge module
# ===================================================
repair-ticket-kre-bridge-test:
  variables:
    MODULE: repair-ticket-kre-bridge
  rules:
    - if: '($CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH =~ /^(feature|fix)\/.+$/ || $CI_COMMIT_BRANCH =~ /^dev\/(feature|fix)\/.+$/) && $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TITLE =~ /(feat|fix|perf|refactor)\(.*\): .*$/'
      changes:
        - repair-ticket-kre-bridge/src/application/**/*
        - repair-ticket-kre-bridge/src/config/**/*
        - repair-ticket-kre-bridge/src/app.py
        - repair-ticket-kre-bridge/Dockerfile
        - repair-ticket-kre-bridge/requirements.txt
    - if: '($CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH =~ /^(feature|fix)\/.+$/ || $CI_COMMIT_BRANCH =~ /^dev\/(feature|fix)\/.+$/) && $CI_PIPELINE_SOURCE == "web"'
  extends: .unittest_module_python
  needs: ["repair-ticket-kre-bridge-linter"]

# ======================================================
# Images builds jobs for repair-ticket-kre-bridge module
# ======================================================
repair-ticket-kre-bridge-build:
  stage: build
  variables:
    MODULE_NAME: repair-ticket-kre-bridge
    MODULE_VERSION: ${CI_PIPELINE_ID}
  rules:
    - if: '($CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH =~ /^dev\/(feature|fix)\/.+$/) && $REPAIR_TICKET_KRE_BRIDGE_DESIRED_TASKS != "0" && $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TITLE =~ /(feat|fix|perf|refactor)\(.*\): .*$/'
      changes:
        - repair-ticket-kre-bridge/src/application/**/*
        - repair-ticket-kre-bridge/src/config/**/*
        - repair-ticket-kre-bridge/src/app.py
        - repair-ticket-kre-bridge/Dockerfile
        - repair-ticket-kre-bridge/requirements.txt
    - if: '($CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH =~ /^dev\/(feature|fix)\/.+$/) && $REPAIR_TICKET_KRE_BRIDGE_DESIRED_TASKS != "0" && $CI_PIPELINE_SOURCE == "web"'
  extends: .build_app_image
  needs:
    - job: repair-ticket-kre-bridge-test
    - job: semantic-release
      optional: true
