apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-outage-monitor-1.fullname" . }}
  labels:
    {{- include "service-outage-monitor-1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "service-outage-monitor-1.selectorLabels" . | nindent 4 }}
