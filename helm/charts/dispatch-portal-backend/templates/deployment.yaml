apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dispatch-portal-backend.fullname" . }}
  labels:
    {{- include "dispatch-portal-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "dispatch-portal-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dispatch-portal-backend.labels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default "automation-master-latest" }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-configmap
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /_health
              port: {{ .Values.config.dispatch_portal_server_port }}
          readinessProbe:
            httpGet:
              path: /_health
              port: {{ .Values.config.dispatch_portal_server_port }}
      {{- if eq .Values.config.mode "local" }}
      imagePullSecrets:
        - name: {{ .Values.config.ecr_registry.name }}
      {{- end }}
