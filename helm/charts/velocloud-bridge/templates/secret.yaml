apiVersion: v1
kind: Secret
metadata:
  name: {{ include "velocloud-bridge.fullname" . }}-secret
  labels:
    {{- include "velocloud-bridge.labels" . | nindent 4 }}
data:
  VELOCLOUD_CREDENTIALS: {{ .Values.config.velocloud_credentials | b64enc }}