apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-outage-monitor-2.fullname" . }}
  labels:
    {{- include "service-outage-monitor-2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "service-outage-monitor-2.selectorLabels" . | nindent 4 }}
