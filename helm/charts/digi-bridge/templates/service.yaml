apiVersion: v1
kind: Service
metadata:
  name: {{ include "digi-bridge.fullname" . }}
  labels:
    {{- include "digi-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "digi-bridge.selectorLabels" . | nindent 4 }}
