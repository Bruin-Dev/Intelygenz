# -- nats helm chart configuration
nats:
  cluster:
    enabled: true
  natsbox:
    enabled: false
  # -- override name to use nats as name for svc, deployment and all created by nats helm chart
  nameOverride: "nats"


prometheus-nats-exporter:
  # enable or disable prometheus-nats-exporter
  enabled: true
  nameOverride: prometheus-nats-exporter
  fullnameOverride: prometheus-nats-exporter
  # if using the Prometheus Operator enable serviceMonitor
  serviceMonitor:
    enabled: true
    additionalLabels: {}
    namespace: prometheus
    interval: "15s"
    scrapeTimeout: "10s"
  # set resources
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  config:
    # Nats data
    nats:
      # See https://github.com/helm/charts/blob/master/stable/nats/templates/monitoring-svc.yaml
      service: automation-engine-nats
      namespace: automation-engine
      port: 8222
    metrics:
      channelz: false
      connz: false
      jsz: false
      gatewayz: false
      routez: false
      serverz: false
      subz: true
      varz: true


## YAML anchor to define capabilities_enabled
.capabilitiesEnabled: &capabilitiesEnabled
  capabilities_enabled:
    # -- Indicate if bruin-bridge is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # bruin-bridge service responds correctly to healthcheck calls.
    bruin_bridge: true
    # -- Indicate if customer-cache is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # customer-cache service responds correctly to healthcheck calls.
    customer_cache: true
    # -- Indicate if digi-bridge is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # digi-bridge service responds correctly to healthcheck calls.
    digi_bridge: true
    # -- Indicate if email-tagger-kre-bridge is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # email-tagger-kre-bridge service responds correctly to healthcheck calls.
    email_tagger_kre_bridge: true
    # -- Indicate if hawkeye-bridge is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # hawkeye-bridge service responds correctly to healthcheck calls.
    hawkeye_bridge: true
    # -- Indicate if hawkeye-customer-cache is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # hawkeye-customer-cache service responds correctly to healthcheck calls.
    hawkeye_customer_cache: true
    # -- Indicate if notifier is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # notifier service responds correctly to healthcheck calls.
    notifier: true
    # -- Indicate if repair-tickets-kre-bridge is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # repair-tickets-kre-bridge service responds correctly to healthcheck calls.
    repair_tickets_kre_bridge: true
    # -- Indicate if t7-bridge is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # t7-bridge service responds correctly to healthcheck calls.
    t7_bridge: true
    # -- Indicate if velocloud-bridge is going to be activated. If it is true an initContainer
    # will be created in the microservice deployment that will wait until the
    # velocloud-bridge service responds correctly to healthcheck calls.
    velocloud_bridge: true


# -- Global configuration for all subcharts
global:
  # -- Name of environment for EKS cluster and network resources
  current_environment: "dev"
  # -- Name of environment for helm charts and
  # redis elasticaches used
  environment: "automation-test"
  # -- NATS cluster endpoint used by bruin-bridge
  nats_server: "nats://nats:4222"
  # -- Redis Hostname used to store heavy NATS messages (>1MB)
  redis_hostname: "redis"
  # -- Redis Hostname used to store information used by customer-cache
  redis_customer_cache_hostname: "redis"
  # -- Redis Hostname used to store metrics obtained from tnba-feedback to train the ML model
  redis_tnba_feedback_hostname: "redis"
  # -- Redis Hostname used to store information used by email-tagger
  redis_email_tagger_hostname: "redis"
  # -- Indicates if the logs will be sent to papertrail or not.
  papertrail_active: "False"
  # -- Papertrail host to which the logs will be sent
  papertrail_host: "logs.papertrailapp.com"
  # -- Papertrail port to which the logs will be sent
  papertrail_port: "0"
  # -- Timezone used for periodic jobs, timestamps...
  timezone: ""
  # -- Contact email address
  last_contact_recipient: ""
  # -- Email account password
  email_acc_pwd: ""
  # -- Indicates if the helm chart will be displayed in an aws or local environment,
  # in case it is local, a specific imagePullSecret will be used to access the images stored in ECR.
  mode: "aws"
  ecr_registry:
    # -- Name of the imagePullSecret created to access the images stored in ECR.
    name: "ecr-registry"


# -- bruin-bridge subchart specific configuration
bruin-bridge:
  # -- Field to indicate if the bruin-bridge module is going to be deployed
  enabled: true
  # -- Number of bruin-bridge pods to do calls to Bruin API.
  # Do not set this below 2 unless is going to deploy in dev or local environment.
  replicaCount: 2
  image:
    # -- Repository image for bruin-bridge module
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/bruin-bridge
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  # -- Service Configuration
  service:
    type: ClusterIP
    port: 5000
  # -- bruin-bridge specific configuration variables
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Client ID credentials for Bruin API
    bruin_client_id: ""
    # -- Client Secret credentials for Bruin API
    bruin_client_secret: ""
    # -- Login URL for Bruin API
    bruin_login_url: ""
    # -- Base URL for Bruin API
    bruin_base_url: ""
    # -- IP of Bruin DEV Login URL
    bruin_login_url_ip: ""
    # -- IP of Bruin DEV Base URL
    bruin_base_url_ip: ""
    metrics:
      # -- Indicates whether the microservice will expose metrics through prometheus.
      enabled: true
      svc:
        port: 9090
        name: metrics
      ## Additional labels for the service monitor
      ## in case you use "serviceMonitorNamespaceSelector" in Prometheus CRD
      labels: { }
      #labels:
      #  servicediscovery: true
  # -- bruin-bridge resource definition
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- customer-cache subchart specific configuration
customer-cache:
  # -- Field to indicate if the customer-cache module is going to be deployed
  enabled: true
  # -- Number of replicas of customer-cache module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- VeloCloud hosts whose edges will be stored to the cache
    velocloud_hosts: []
    # -- E-mail address that will get e-mails with a relation of service numbers that have multiple Bruin inventories
    duplicate_inventories_recipient: ""
    # -- Defines how often the cache is refreshed
    refresh_job_interval: 0
    # -- Defines how often the next refresh flag is checked to decide if it's time to refresh the cache or not
    refresh_check_interval: 0
    # -- VeloCloud edges that should be ignored in the caching process
    blacklisted_edges: []
    # -- Client IDs whose edges have Pending management status that should be ignored in the caching process
    blacklisted_clients_with_pending_status: []
    # -- Management statuses that should be considered in the caching process
    whitelisted_management_statuses: []
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/customer-cache
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 400m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi


# -- digi-bridge subchart specific configuration
digi-bridge:
  # -- Field to indicate if the digi-bridge module is going to be deployed
  enabled: true
  # -- Number of replicas of digi-bridge module
  replicaCount: 1
  # -- digi-bridge specific configuration variables
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Client ID credentials for Digi API
    digi_client_id: ""
    # -- Client Secret credentials for Digi API
    digi_client_secret: ""
    # -- Base URL for Digi API
    digi_base_url: ""
    # -- Auth tokens TTL
    digi_token_ttl: 0
    # -- IP for Digi Environment
    digi_api_ip: ""
    # -- Record name for Digi Production Environment
    digi_api_dns_record_name: ""
    # -- IP for Digi Test Environment
    digi_api_test_ip: ""
    # -- Record name for Digi Test Environment
    digi_api_test_dns_record_name: ""
  # -- digi-bridge image details
  image:
    # -- digi-bridge image image
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/digi-bridge
    pullPolicy: Always
    # -- digi-bridge tag of docker image
    tag: ""
  # -- digi-bridge service details
  service:
    type: ClusterIP
    port: 5000
  # -- digi-bridge resource definition
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- digi-bridge autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 2
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80


# -- digi-reboot-report subchart specific configuration
digi-reboot-report:
  # -- Field to indicate if the digi-reboot-report module is going to be deployed
  enabled: true
  # -- Number of replicas of digi-reboot-report module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Defines how often the report is built and sent
    job_interval: 0
    # -- Defines how much time back to look for DiGi Reboot logs
    logs_lookup_interval: 0
    # -- Email address to send the report
    recipient: ""
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/digi-reboot-report
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 400m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi


# -- dri-bridge subchart specific configuration
dri-bridge:
  # -- Field to indicate if the dri-bridge module is going to be deployed
  enabled: true
  # -- Number of replicas of dri-bridge module
  replicaCount: 1
  # -- dri-bridge specific configuration variables
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Username to log into Dri API
    dri_acc_email: ""
    # -- Password to log into Dri API
    dri_acc_password: ""
    # -- Base URL for Dri API
    dri_base_url: ""
    metrics:
      # -- Indicates whether the microservice will expose metrics through prometheus.
      enabled: true
      svc:
        port: 9090
        name: metrics
      ## Additional labels for the service monitor
      ## in case you use "serviceMonitorNamespaceSelector" in Prometheus CRD
      labels: {}
      #labels:
      #  servicediscovery: true
  # -- dri-bridge image details
  image:
    # -- dri-bridge image image
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/dri-bridge
    pullPolicy: Always
    # -- dri-bridge tag of docker image
    tag: ""
  # -- dri-bridge service details
  service:
    type: ClusterIP
    port: 5000
  # -- dri-bridge resource definition
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- dri-bridge autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 2
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80


# -- ecr-cred-helper subchart specific configuration
ecr-cred-helper:
  enabled: false
  # -- Specific configuration for ecr-cred-helper
  config:
    mode: "aws"
    # -- Secret Access Key of AWS Account
    aws_secret_access_key: ""
    # -- Access Key Id of AWS Account
    aws_access_key_id: ""
    # -- AWS Region of account
    aws_region: "us-east-1"
    # -- awscli version to use for get credentials
    awscli_version: "1.16.257"
    # -- Kubectl version
    kubectl_version: "1.17.0"
    ecr_registry:
      url: ""
      password: ""
      name: "ecr-registry"
    schedule_expr: "0 */6 * * *"
  image:
    repository: alpine
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: "3.12.0"
  imagePullSecrets: [ ]
  nameOverride: ""
  fullnameOverride: ""
  serviceAccount:
    # Specifies whether a service account should be created
    create: true
    # Annotations to add to the service account
    annotations: { }
    # The name of the service account to use.
    # If not set and create is true, a name is generated using the fullname template
    name: "sa-ecr-cred-helper"
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- email-tagger-kre-bridge subchart specific configuration
email-tagger-kre-bridge:
  # -- Field to indicate if the email-tagger-kre-bridge module is going to be deployed
  enabled: true
  # -- Number of replicas of email-tagger-kre-bridge module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Base URL for E-mail Tagger's KRE
    kre_base_url: ""
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/email-tagger-kre-bridge
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- email-tagger-kre-bridge autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 2
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80


# -- email-tagger-monitor subchart specific configuration
email-tagger-monitor:
  # -- Field to indicate if the email-tagger-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of email-tagger-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Defines how often new emails received from Bruin are processed
    new_emails_job_interval: 0
    # -- Defines how often new tickets received from Bruin are sent to the KRE to train the AI model
    new_tickets_job_interval: 0
    # -- Defines how many simultaneous emails are processed
    max_concurrent_emails: 0
    # -- Defines how many simultaneous tickets are sent to the KRE to train the AI model
    max_concurrent_tickets: 0
    # -- API request key for incoming requests
    api_request_key: ""
    # -- API signature secret key for incoming requests
    api_request_signature_secret_key: ""
    # -- API server endpoint prefix for incoming requests
    api_endpoint_prefix: ""
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/email-tagger-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 400m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi


# -- fraud-monitor subchart specific configuration
fraud-monitor:
  # -- Field to indicate if the fraud-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of fraud-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/fraud-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 400m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi


# -- hawkeye-affecting-monitor subchart specific configuration
hawkeye-affecting-monitor:
  # -- Field to indicate if the hawkeye-affecting-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of hawkeye-affecting-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Defines how often devices are checked to find and report issues
    monitoring_job_interval: 0
    # -- Defines how much time back to look for probes' tests results
    probes_tests_results_lookup_interval: 0
    # -- Bruin's product category under monitoring
    monitored_product_category: ""
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/hawkeye-affecting-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 400m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi


# -- hawkeye-bridge subchart specific configuration
hawkeye-bridge:
  # -- Field to indicate if the hawkeye-bridge module is going to be deployed
  enabled: true
  # -- Number of replicas of hawkeye-bridge module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Client username to log into Hawkeye API
    client_username: ""
    # -- Client password to log into Hawkeye API
    client_password: ""
    # -- Base URL to access Hawkeye API
    base_url: ""
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/hawkeye-bridge
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- hawkeye-bridge autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 2
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80


# -- hawkeye-customer-cache subchart specific configuration
hawkeye-customer-cache:
  # -- Field to indicate if the hawkeye-customer-cache module is going to be deployed
  enabled: true
  # -- Number of replicas of hawkeye-customer-cache module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- E-mail address that will get e-mails with a relation of service numbers that have multiple Bruin inventories
    duplicate_inventories_recipient: ""
    # -- Defines how often the cache is refreshed
    refresh_job_interval: 0
    # -- Management statuses that should be considered in the caching process
    whitelisted_management_statuses: []
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/hawkeye-customer-cache
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 400m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi


# -- hawkeye-outage-monitor subchart specific configuration
hawkeye-outage-monitor:
  # -- Field to indicate if the hawkeye-outage-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of hawkeye-outage-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Defines how often devices are checked to find and report issues
    monitoring_job_interval: 0
    # -- Defines how much time to wait before checking if a particular device is still in outage state
    quarantine_for_devices_in_outage: 0
    # -- Bruin's product category under monitoring
    monitored_product_category: ""
    # -- Defines for how long a ticket can be auto-resolved after the last documented outage
    grace_period_to_autoresolve_after_last_documented_outage: 0
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/hawkeye-outage-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- intermapper-outage-monitor subchart specific configuration
intermapper-outage-monitor:
  # -- Field to indicate if the intermapper-outage-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of intermapper-outage-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Defines how often InterMapper events are checked to find and report issues
    monitoring_job_interval: 0
    # -- E-mail account that receives InterMapper events for later analysis
    observed_inbox_email_address: ""
    # -- Senders addresses whose e-mail messages represent InterMapper events
    observed_inbox_senders: []
    # -- InterMapper events considered as DOWN
    monitored_down_events: []
    # -- InterMapper events considered as UP
    monitored_up_events: []
    # -- Defines how many simultaneous email batches related to the same InterMapper asset are processed
    max_concurrent_email_batches: 0
    # -- Defines which Bruin product categories are taken into account when auto-resolving tickets
    whitelisted_product_categories_for_autoresolve: []
    # -- Defines for how long a ticket can be auto-resolved after the last documented outage
    grace_period_to_autoresolve_after_last_documented_outage: 0
    # -- Parameters to fetch from DRI to include them in InterMapper notes for PIAB devices
    dri_parameters_for_piab_notes: []
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/intermapper-outage-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- last-contact-report subchart specific configuration
last-contact-report:
  # -- Field to indicate if the last-contact-report module is going to be deployed
  enabled: true
  # -- Number of replicas of last-contact-report module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- VeloCloud hosts whose edges will be used to build the report
    monitored_velocloud_hosts: []
    # -- Email address to send the report
    recipient: ""
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/last-contact-report
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- links-metrics-api subchart specific configuration
links-metrics-api:
  enabled: true
  # -- Number of links-metrics-api pods
  replicaCount: 1
  config:
    papertrail_prefix: ""
    # -- Indicate mongo username
    mongodb_username: ""
    # -- Indicate mongo password
    mongodb_password: ""
    # -- Indicate mongo hostname
    mongodb_host: ""
    # -- Indicate mongo port to use
    mongodb_port: ""
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/links-metrics-api
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: LoadBalancer
    port: 5000
    securityGroup: ""
    awsCertArn: ""
  resources:
    limits:
      cpu: 400m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi


# -- links-metrics-collector subchart specific configuration
links-metrics-collector:
  enabled: true
  # -- Number of links-metrics-collector pods
  replicaCount: 1
  config:
    papertrail_prefix: ""
    # -- Indicate mongo username
    mongodb_username: ""
    # -- Indicate mongo password
    mongodb_password: ""
    # -- Indicate mongo hostname
    mongodb_host: ""
    # -- Indicate mongo port to use
    mongodb_port: ""
    # -- Indicate the whitelist source range CIDR to allow access
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/links-metrics-collector
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 400m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi


# -- lumin-billing-report subchart specific configuration
lumin-billing-report:
  # -- Field to indicate if the lumin-billing-report module is going to be deployed
  enabled: true
  # -- Number of replicas of lumin-billing-report module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- URI of Lumin API
    lumin_uri: ""
    # -- Token credentials for Lumin API
    lumin_token: ""
    # -- Name of customer to generate lumin-billing-report
    customer_name: ""
    # -- Email address to send lumin-billing-report
    billing_recipient: ""
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/lumin-billing-report
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- notifier subchart specific configuration
notifier:
  # -- Field to indicate if the notifier module is going to be deployed
  enabled: true
  # -- Number of replicas of notifier module
  replicaCount: 1
  # -- notifier image details
  image:
    # -- notifier repository for docker images
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/notifier
    pullPolicy: Always
    # -- notifier tag of docker image
    tag: ""
  # -- notifier Service Configuration
  service:
    type: ClusterIP
    port: 5000
  # -- notifier specific configuration variables
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Slack webhook to send messages
    slack_webhook: ""
    # -- Email account used to send messages to other accounts (username)
    email_account_for_message_delivery_username: ""
    # -- Email account used to send messages to other accounts (password)
    email_account_for_message_delivery_password: ""
    # -- Mapping of e-mail addresses and passwords whose inboxes can be read for later analysis
    monitorable_email_accounts: {}
  resources:
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- notifier autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 2
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80

# -- repair-tickets-kre-bridge subchart specific configuration
repair-tickets-kre-bridge:
  # -- Field to indicate if the repair-tickets-kre-bridge module is going to be deployed
  enabled: true
  # -- Number of replicas of repair-tickets-kre-bridge module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Base URL for RTA's KRE
    kre_base_url: ""
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/repair-tickets-kre-bridge
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- repair-tickets-kre-bridge autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 2
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80

# -- repair-tickets-monitor subchart specific configuration
repair-tickets-monitor:
  # -- Field to indicate if the repair-tickets-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of repair-tickets-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Defines how often new emails tagged by the E-mail Tagger are processed
    rta_monitor_job_interval: 0
    # -- Defines how often new created tickets fetched from Bruin are sent to the KRE to train the AI model
    new_created_tickets_feedback_job_interval: 0
    # -- Defines how many simultaneous tagged emails are processed
    max_concurrent_emails_for_monitoring: 0
    # -- Defines how many simultaneous new created tickets are sent to the KRE to train the AI model
    max_concurrent_created_tickets_for_feedback: 0
    # -- Defines how many simultaneous new closed tickets are sent to the KRE to train the AI model
    max_concurrent_closed_tickets_for_feedback: 0
    # -- Mapping of tag names and their corresponding numeric ID, as defined in the AI model
    tag_ids_mapping: {}
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
    metrics:
      # -- Indicates whether the microservice will expose metrics through prometheus.
      enabled: true
      svc:
        port: 9090
        name: metrics
      ## Additional labels for the service monitor
      ## in case you use "serviceMonitorNamespaceSelector" in Prometheus CRD
      labels: {}
      #labels:
      #  servicediscovery: true
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/repair-tickets-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 300m
      memory: 384Mi
    requests:
      cpu: 150m
      memory: 192Mi


# -- service-affecting-monitor subchart specific configuration
service-affecting-monitor:
  # -- Field to indicate if the service-affecting-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of service-affecting-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Indicates if the monitor reports process will be executed on start or not
    exec_monitor_reports_on_start: "False"
    # -- Indicates if the bandwidth reports process will be executed on start or not
    exec_bandwidth_reports_on_start: "False"
    # -- Bruin's product category under monitoring
    monitored_product_category: ""
    # -- [Monitoring] Defines how often devices are checked to find and report issues
    monitoring__monitoring_job_interval: 0
    # -- [Monitoring] VeloCloud hosts whose edges will be monitored
    monitoring__monitored_velocloud_hosts: []
    # -- [Monitoring] Mapping of VeloCloud hosts, Bruin customers and default contact info
    monitoring__default_contact_info_per_customer: {}
    # -- [Monitoring] Threshold for Latency troubles
    monitoring__latency_monitoring_threshold: 0
    # -- [Monitoring] Threshold for Packet Loss troubles
    monitoring__packet_loss_monitoring_threshold: 0
    # -- [Monitoring] Threshold for Jitter troubles
    monitoring__jitter_monitoring_threshold: 0
    # -- [Monitoring] Threshold for Bandwidth Over Utilization troubles
    monitoring__bandwidth_over_utilization_monitoring_threshold: 0
    # -- [Monitoring] Threshold for Circuit Instability troubles
    monitoring__circuit_instability_monitoring_threshold: 0
    # -- [Monitoring] Max DOWN events allowed in Circuit Instability checks while auto-resolving tickets
    monitoring__circuit_instability_autoresolve_threshold: 0
    # -- [Monitoring] Defines how much time back to look for Latency metrics in Latency checks
    monitoring__latency_monitoring_lookup_interval: 0
    # -- [Monitoring] Defines how much time back to look for Packet Loss metrics in Packet Loss checks
    monitoring__packet_loss_monitoring_lookup_interval: 0
    # -- [Monitoring] Defines how much time back to look for Jitter metrics in Jitter checks
    monitoring__jitter_monitoring_lookup_interval: 0
    # -- [Monitoring] Defines how much time back to look for Bandwidth metrics in Bandwidth Over Utilization checks
    monitoring__bandwidth_over_utilization_monitoring_lookup_interval: 0
    # -- [Monitoring] Defines how much time back to look for DOWN events in Circuit Instability checks
    monitoring__circuit_instability_monitoring_lookup_interval: 0
    # -- [Monitoring] Defines how much time back to look for all kinds of metrics while running auto-resolves
    monitoring__autoresolve_lookup_interval: 0
    # -- [Monitoring] Defines for how long a ticket can be auto-resolved after the last documented trouble
    monitoring__grace_period_to_autoresolve_after_last_documented_trouble: 0
    # -- [Monitoring] Defines how many times a ticket can be auto-resolved
    monitoring__max_autoresolves_per_ticket: 0
    # -- [Monitoring] List of client IDs for which Bandwidth Over Utilization checks are enabled
    monitoring__customers_with_bandwidth_over_utilization_monitoring: []
    # -- [Monitoring] List of link labels that are excluded from forwards to the ASR queue
    monitoring__link_labels_blacklisted_in_asr_forwards: []
    # -- [Reoccurring Trouble Report] Cron expression that determines when to build and deliver this report
    reoccurring_trouble_report__execution_cron_expression: ""
    # -- [Reoccurring Trouble Report] Troubles that will be reported
    reoccurring_trouble_report__reported_troubles: []
    # -- [Reoccurring Trouble Report] Defines how much time back to look for Bruin tickets
    reoccurring_trouble_report__tickets_lookup_interval: 0
    # -- [Reoccurring Trouble Report] Number of different tickets a trouble must appear in for a particular edge and interface to include it in the report
    reoccurring_trouble_report__reoccurring_trouble_tickets_threshold: 0
    # -- [Reoccurring Trouble Report] Mapping of Bruin customer IDs and recipients of these reports
    reoccurring_trouble_report__recipients_per_customer: {}
    # -- [Daily Bandwidth Report] Cron expression that determines when to build and deliver this report
    daily_bandwidth_report__execution_cron_expression: ""
    # -- [Daily Bandwidth Report] Defines how much time back to look for bandwidth metrics and Bruin tickets
    daily_bandwidth_report__lookup_interval: 0
    # -- [Daily Bandwidth Report] Customers for whom this report will be built
    daily_bandwidth_report__enabled_customers: []
    # -- [Daily Bandwidth Report] List of recipients that will get these reports
    daily_bandwidth_report__recipients: []
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
    metrics:
      # -- Indicates whether the microservice will expose metrics through prometheus.
      enabled: true
      svc:
        port: 9090
        name: metrics
      ## Additional labels for the service monitor
      ## in case you use "serviceMonitorNamespaceSelector" in Prometheus CRD
      labels: {}
      #labels:
      #  servicediscovery: true
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/service-affecting-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 300m
      memory: 384Mi
    requests:
      cpu: 150m
      memory: 192Mi


# -- service-outage-monitor (SOM) subchart specific configuration
service-outage-monitor:
  # -- Field to indicate if the service-outage-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of service-outage-monitor module
  replicaCount: 1
  config:
    # -- Bruin's product category under monitoring
    monitored_product_category: ""
    # -- [Monitoring] Defines how often devices are checked to find and report issues
    monitoring__monitoring_job_interval: 0
    # -- [Monitoring] VeloCloud hosts whose edges will be monitored
    monitoring__monitored_velocloud_hosts: []
    # -- [Monitoring] Defines how much time to wait before re-checking an edge currently in Link Down state
    monitoring__quarantine_for_edges_in_link_down_outage: 0
    # -- [Monitoring] Defines how much time to wait before re-checking an edge currently in Hard Down state
    monitoring__quarantine_for_edges_in_hard_down_outage: 0
    # -- [Monitoring] Defines how much time to wait before re-checking an edge currently in Link Down (HA) state
    monitoring__quarantine_for_edges_in_ha_link_down_outage: 0
    # -- [Monitoring] Defines how much time to wait before re-checking an edge currently in Soft Down (HA) state
    monitoring__quarantine_for_edges_in_ha_soft_down_outage: 0
    # -- [Monitoring] Defines how much time to wait before re-checking an edge currently in Hard Down (HA) state
    monitoring__quarantine_for_edges_in_ha_hard_down_outage: 0
    # -- [Monitoring] E-mail address that will receive a tiny report showing which edges from VeloCloud responses are not in the cache of customers
    monitoring__missing_edges_from_cache_report_recipient: ""
    # -- [Monitoring] List of link labels that are excluded from forwards to the ASR queue
    monitoring__link_labels_blacklisted_in_asr_forwards: []
    # -- [Monitoring] List of edges that are excluded from Service Outage monitoring
    monitoring__blacklisted_edges: []
    # -- [Monitoring] Defines for how long a ticket can be auto-resolved after the last documented outage
    monitoring__grace_period_to_autoresolve_after_last_documented_outage: 0
    # -- [Monitoring] Defines for how long the monitor will wait before attempting a new DiGi Reboot on an edge
    monitoring__grace_period_before_attempting_new_digi_reboots: 0
    # -- [Monitoring] Severity level for Edge Down outages
    monitoring__severity_level_for_edge_down_outages: 0
    # -- [Monitoring] Severity level for Link Down outages
    monitoring__severity_level_for_link_down_outages: 0
    # -- [Monitoring] Defines how many times a ticket can be auto-resolved
    monitoring__max_autoresolves_per_ticket: 0
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
    metrics:
      # -- Indicates whether the microservice will expose metrics through prometheus.
      enabled: true
      svc:
        port: 9090
        name: metrics
      ## Additional labels for the service monitor
      ## in case you use "serviceMonitorNamespaceSelector" in Prometheus CRD
      labels: {}
      #labels:
      #  servicediscovery: true
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/service-outage-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 300m
      memory: 384Mi
    requests:
      cpu: 150m
      memory: 192Mi


# -- service-outage-monitor-triage subchart specific configuration
service-outage-monitor-triage:
  # -- Field to indicate if the service-outage-monitor-triage module is going to be deployed
  enabled: true
  # -- Number of replicas of service-outage-monitor-triage module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Bruin's product category under monitoring
    monitored_product_category: ""
    # -- [Triage] Defines how often tickets are checked to see if it needs an initial triage or events note
    triage__monitoring_job_interval: 0
    # -- [Triage] VeloCloud hosts whose edges will be monitored
    triage__monitored_velocloud_hosts: []
    # -- [Triage] Defines how many events will be included in events notes
    triage__max_events_per_event_note: 0
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
    metrics:
      # -- Indicates whether the microservice will expose metrics through prometheus.
      enabled: true
      svc:
        port: 9090
        name: metrics
      ## Additional labels for the service monitor
      ## in case you use "serviceMonitorNamespaceSelector" in Prometheus CRD
      labels: {}
      #labels:
      #  servicediscovery: true
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/service-outage-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 300m
      memory: 384Mi
    requests:
      cpu: 150m
      memory: 192Mi


# -- sites-monitor subchart specific configuration
sites-monitor:
  # -- Field to indicate if the sites-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of sites-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Defines how often to look for links and edges, and write their data to the metrics server
    monitoring_job_interval: 0
    # -- VeloCloud hosts whose edges will be monitored
    monitored_velocloud_hosts: []
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
    metrics:
      # -- Indicates whether the microservice will expose metrics through prometheus.
      enabled: true
      svc:
        port: 9090
        name: metrics
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/sites-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- t7-bridge subchart specific configuration
t7-bridge:
  # -- Field to indicate if the t7-bridge module is going to be deployed
  enabled: true
  # -- Number of replicas of t7-bridge module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Base URL for TNBA's KRE
    kre_base_url: ""
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/t7-bridge
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  imagePullSecrets: [ ]
  nameOverride: ""
  fullnameOverride: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- t7-bridge autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 2
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80


# -- ticket-collector subchart specific configuration
ticket-collector:
  # -- Field to indicate if the ticket-collector module is going to be deployed
  enabled: true
  # -- Number of replicas of ticket-collector module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Indicate the interval task that must run in parallel
    interval_tasks_run: ""
    # -- Indicate mongo username
    mongodb_username: ""
    # -- Indicate mongo password
    mongodb_password: ""
    # -- Indicate mongo hostname
    mongodb_host: ""
    # -- Indicate mongo database to use
    mongodb_database: ""
    # -- Indicate bruin client id to access
    bruin_client_id: ""
    # -- Indicate bruin client secret
    bruin_client_secret: ""
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/ticket-collector
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- ticket-statistics subchart specific configuration
ticket-statistics:
  # -- Field to indicate if the ticket-statistics module is going to be deployed
  enabled: true
  # -- Number of replicas of ticket-statistics module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Indicate mongo username
    mongodb_username: ""
    # -- Indicate mongo password
    mongodb_password: ""
    # -- Indicate mongo hostname
    mongodb_host: ""
    # -- Indicate mongo database to use
    mongodb_database: ""
    # -- Indicate ticket statistics server port
    server_port: ""
    # -- Indicate ticket statistics server root path
    server_root_path: ""
    # -- Indicate ticket statistics server version
    server_version: ""
    # -- Indicate ticket statistics server name
    server_name: ""
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/ticket-statistics
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- tnba-feedback subchart specific configuration
tnba-feedback:
  # -- Field to indicate if the tnba-feedback module is going to be deployed
  enabled: true
  # -- Number of replicas of tnba-feedback module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Bruin's product category under monitoring
    monitored_product_category: ""
    # -- Defines how often tickets are pulled from Bruin and sent to the KRE to train the predictive model
    feedback_job_interval: 0
    # -- VeloCloud hosts whose edges will be monitored
    monitored_velocloud_hosts: []
    # -- Defines for how long a ticket needs to wait before being re-sent to the KRE
    grace_period_before_resending_tickets: 0
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/tnba-feedback
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- tnba-monitor subchart specific configuration
tnba-monitor:
  # -- Field to indicate if the tnba-monitor module is going to be deployed
  enabled: true
  # -- Number of replicas of tnba-monitor module
  replicaCount: 1
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Bruin's product category under monitoring
    monitored_product_category: ""
    # -- Defines how often tickets are checked to see if they need a new TNBA note
    monitoring_job_interval: 0
    # -- VeloCloud hosts whose edges will be monitored
    monitored_velocloud_hosts: []
    # -- List of edges that are excluded from TNBA monitoring
    blacklisted_edges: []
    # -- Defines for how long a ticket needs to wait since it was opened before appending a new TNBA note
    grace_period_before_appending_new_tnba_notes: 0
    # -- Defines for how long a Service Outage ticket needs to wait after the last documented outage to get a new TNBA note appended
    grace_period_before_monitoring_tickets_based_on_last_documented_outage: 0
    # -- Defines the minimum confidence level required to consider a Request Completed / Repair Completed prediction accurate in TNBA auto-resolves
    min_required_confidence_for_request_and_repair_completed_predictions: 0
    # -- Indicate the capabilities dependencies
    <<: *capabilitiesEnabled
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/tnba-monitor
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi


# -- velocloud-bridge subchart specific configuration
velocloud-bridge:
  # -- Field to indicate if the velocloud-bridge module is going to be deployed
  enabled: true
  # -- Number of replicas of velocloud-bridge module
  replicaCount: 2
  config:
    # -- Papertrail prefix for create logs definition
    papertrail_prefix: ""
    # -- Velocloud credentials
    velocloud_credentials: ""
  image:
    repository: 374050862540.dkr.ecr.us-east-1.amazonaws.com/velocloud-bridge
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  service:
    type: ClusterIP
    port: 5000
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- velocloud-bridge autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 3
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
