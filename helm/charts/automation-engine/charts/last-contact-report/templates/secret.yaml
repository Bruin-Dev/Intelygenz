apiVersion: v1
kind: Secret
metadata:
  name: {{ include "last-contact-report.secretName" . }}
  labels:
    {{- include "last-contact-report.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
