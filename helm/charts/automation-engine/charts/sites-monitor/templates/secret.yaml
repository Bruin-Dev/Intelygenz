apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sites-monitor.secretName" . }}
  labels:
    {{- include "sites-monitor.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
  MONITORED_VELOCLOUD_HOSTS: {{ mustToJson .Values.config.monitored_velocloud_hosts | b64enc }}