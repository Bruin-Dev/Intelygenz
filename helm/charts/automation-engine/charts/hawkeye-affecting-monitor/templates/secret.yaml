apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hawkeye-affecting-monitor.secretName" . }}
  labels:
    {{- include "hawkeye-affecting-monitor.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
  MONITORED_PRODUCT_CATEGORY: {{ .Values.config.monitored_product_category | b64enc }}