{{- if and .Values.global.externalSecrets.enabled -}}
apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: {{ include "dri-bridge.secretName" . }}
  labels:
    {{- include "dri-bridge.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
spec:
  secretStoreRef:
    name: {{ .Values.global.environment }}-parameter-store
    kind: SecretStore 
  target:
    creationPolicy: 'Owner'
  # Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h" (from time.ParseDuration)
  # May be set to zero to fetch and create it 
  {{- if eq .Values.global.current_environment "dev" }}
  refreshInterval: "0"
  {{ else }}
  refreshInterval: "5m"  
  {{- end }}
  data:
    {{- with .Values.global.externalSecrets.envPath }}
    # GLOBAL CONFIG
    - secretKey: NATS_SERVER1
      remoteRef:
        key: {{ . }}/nats/endpoint-url
    - secretKey: PAPERTRAIL_ACTIVE
      remoteRef:
        key: {{ . }}/papertrail/enabled
    - secretKey: PAPERTRAIL_HOST
      remoteRef:
        key: {{ . }}/papertrail/host
    - secretKey: PAPERTRAIL_PORT
      remoteRef:
        key: {{ . }}/papertrail/port
    - secretKey: REDIS_HOSTNAME
      remoteRef:
        key: {{ . }}/redis/main-hostname
    - secretKey: TIMEZONE
      remoteRef:
        key: {{ . }}/timezone
    # SPECIFIC CONFIG
    - secretKey: BASE_URL
      remoteRef:
        key: {{ . }}/dri-bridge/base-url
    - secretKey: DRI_DATA_REDIS_TTL
      remoteRef:
        key: {{ . }}/dri-bridge/dri-data-redis-ttl
    - secretKey: PASSWORD
      remoteRef:
        key: {{ . }}/dri-bridge/password
    - secretKey: USERNAME
      remoteRef:
        key: {{ . }}/dri-bridge/username
    {{- end }}
{{- end }}
