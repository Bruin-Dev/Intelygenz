apiVersion: v1
kind: Service
metadata:
  name: {{ include "cts-bridge.fullname" . }}
  labels:
    {{- include "cts-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "cts-bridge.selectorLabels" . | nindent 4 }}
