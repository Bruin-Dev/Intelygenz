apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "velocloud-gateway-monitor.fullname" . }}-test-connection"
  labels:
    {{- include "velocloud-gateway-monitor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "velocloud-gateway-monitor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
