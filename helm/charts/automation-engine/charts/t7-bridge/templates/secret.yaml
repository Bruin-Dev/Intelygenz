apiVersion: v1
kind: Secret
metadata:
  name: {{ include "t7-bridge.secretName" . }}
  labels:
    {{- include "t7-bridge.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
