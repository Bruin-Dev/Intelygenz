apiVersion: v1
kind: Service
metadata:
  name: {{ include "dispatch-portal-backend.fullname" . }}
  labels:
    {{- include "dispatch-portal-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "dispatch-portal-backend.selectorLabels" . | nindent 4 }}
