apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dispatch-portal-backend.fullname" . }}-test-connection"
  labels:
    {{- include "dispatch-portal-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dispatch-portal-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
