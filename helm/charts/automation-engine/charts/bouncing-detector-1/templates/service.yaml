apiVersion: v1
kind: Service
metadata:
  name: {{ include "bouncing-detector-1.fullname" . }}
  labels:
    {{- include "bouncing-detector-1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
    {{- if .Values.config.metrics.enabled }}
    - name: {{ .Values.config.metrics.svc.name }}
      port: {{ .Values.config.metrics.svc.port }}
    {{- end }}
  selector:
    {{- include "bouncing-detector-1.selectorLabels" . | nindent 4 }}
