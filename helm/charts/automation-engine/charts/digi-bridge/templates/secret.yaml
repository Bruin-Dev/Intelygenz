apiVersion: v1
kind: Secret
metadata:
  name: {{ include "digi-bridge.secretName" . }}
  labels:
    {{- include "digi-bridge.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
