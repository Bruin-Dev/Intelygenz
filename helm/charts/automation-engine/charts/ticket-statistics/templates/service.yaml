apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticket-statistics.fullname" . }}
  labels:
    {{- include "ticket-statistics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "ticket-statistics.selectorLabels" . | nindent 4 }}
