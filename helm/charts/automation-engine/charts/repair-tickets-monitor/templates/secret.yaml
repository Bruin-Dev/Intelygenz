apiVersion: v1
kind: Secret
metadata:
  name: {{ include "repair-tickets-monitor.secretName" . }}
  labels:
    {{- include "repair-tickets-monitor.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
  TAG_IDS_MAPPING: {{ mustToJson .Values.config.tag_ids_mapping | b64enc }}
