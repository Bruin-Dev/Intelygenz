apiVersion: v1
kind: Secret
metadata:
  name: {{ include "velocloud-bridge.secretName" . }}
  labels:
    {{- include "velocloud-bridge.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
