apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticket-collector.fullname" . }}
  labels:
    {{- include "ticket-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
    {{- if .Values.config.metrics.enabled }}
    - name: {{ .Values.config.metrics.svc.name }}
      port: {{ .Values.config.metrics.svc.port }}
    {{- end }}
  selector:
    {{- include "ticket-collector.selectorLabels" . | nindent 4 }}
