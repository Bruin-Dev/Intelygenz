apiVersion: v1
kind: Secret
metadata:
  name: {{ include "email-tagger-kre-bridge.secretName" . }}
  labels:
    {{- include "email-tagger-kre-bridge.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
  KRE_BASE_URL: {{ .Values.config.kre_base_url | b64enc }}
