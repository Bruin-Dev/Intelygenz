apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dispatch-portal-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "dispatch-portal-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dispatch-portal-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
