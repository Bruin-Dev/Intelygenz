apiVersion: v1
kind: Secret
metadata:
  name: {{ include "notifier.secretName" . }}
  labels:
    {{- include "notifier.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/match: "true"
data:
