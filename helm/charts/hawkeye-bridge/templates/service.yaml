apiVersion: v1
kind: Service
metadata:
  name: {{ include "hawkeye-bridge.fullname" . }}
  labels:
    {{- include "hawkeye-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "hawkeye-bridge.selectorLabels" . | nindent 4 }}
