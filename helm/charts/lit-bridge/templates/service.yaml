apiVersion: v1
kind: Service
metadata:
  name: {{ include "lit-bridge.fullname" . }}
  labels:
    {{- include "lit-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lit-bridge.selectorLabels" . | nindent 4 }}
