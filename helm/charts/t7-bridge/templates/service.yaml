apiVersion: v1
kind: Service
metadata:
  name: {{ include "t7-bridge.fullname" . }}
  labels:
    {{- include "t7-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "t7-bridge.selectorLabels" . | nindent 4 }}
