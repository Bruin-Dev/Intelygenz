apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-outage-monitor-4.fullname" . }}
  labels:
    {{- include "service-outage-monitor-4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "service-outage-monitor-4.selectorLabels" . | nindent 4 }}
