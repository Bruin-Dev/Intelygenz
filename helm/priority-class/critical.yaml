apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: critical-priority
value: 4000
globalDefault: false
description: "This priority class should be used for CRITICAL PRIORITY pods (deploys) only."