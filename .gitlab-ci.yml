stages:
  - test

test:
  image: python:3.6
  before_script:
    - apt-get update -q -y
    - pip install -r bruin-bridge/requirements.txt
  script:
    - python3.6 -m coverage run bruin-bridge/app_test.py
    - python3.6 -m coverage report bruin-bridge/app.py