stages:
  - validation
  - unit_test
  - acceptance_tests
  - basic_infra
  - build
  - deploy
  - grafana_user_creation

variables:
  NATS_SERVER1: "nats://nats-streaming:4222"
  NATS_CLUSTER_NAME: "automation-engine-nats"
  NATS_MODULE_VERSION: 0.15.1
  DOCKER_HOST: tcp://docker:2375/
  DOCKER_DRIVER: overlay2
  REPOSITORY_URL: 374050862540.dkr.ecr.us-east-1.amazonaws.com/automation

#
# Using gitlab-ci anchors to reuse scripts between jobs (https://docs.gitlab.com/ee/ci/yaml/#anchors)
#
.job_linter_template: &lint_module
  stage: validation
  image: python:3.6
  before_script:
    - apt-get update -q -y
    - pip install pycodestyle
  script:
    - pycodestyle --max-line-length=120 ${MODULE}

.job_unittest_template: &unittest_module
  stage: unit_test
  image: python:3.6
  before_script:
    - apt-get update -q -y
    - cd ${MODULE}
    - pip install -r requirements.txt
  script:
    - pytest --cov

.job_acceptance_test_template: &acceptance_test_module
  stage: acceptance_tests
  image: docker:18-git
  services:
    - docker:18-dind
  before_script:
    - apk add --no-cache py-pip python-dev libffi-dev openssl-dev gcc libc-dev make
    - pip install docker-compose
  script:
    - docker-compose -f docker-compose-ci.yml up --build --abort-on-container-exit
  after_script:
    - docker-compose -f docker-compose-ci.yml down && docker-compose -f docker-compose-ci.yml rm -v

.job_app_build_template: &build_app_image
  services:
    - docker:18-dind
  before_script:
    - apk add --no-cache curl jq python py-pip
    - pip install awscli
    - $(aws ecr get-login --no-include-email --region us-east-1)
  script:
    - docker build -t ${REPOSITORY_URL}-${MODULE_NAME}:latest -t ${REPOSITORY_URL}-${MODULE_NAME}:${MODULE_VERSION} -f ${MODULE_NAME}/Dockerfile .
    - docker push ${REPOSITORY_URL}-${MODULE_NAME}:latest
    - docker push ${REPOSITORY_URL}-${MODULE_NAME}:${MODULE_VERSION}
  after_script:
    - docker rmi ${REPOSITORY_URL}-${MODULE_NAME}:latest
    - docker rmi ${REPOSITORY_URL}-${MODULE_NAME}:${MODULE_VERSION}


.job_terraform_template: &terraform_template
  image:
    name: hashicorp/terraform:0.12.7
    entrypoint:
      - '/usr/bin/env'
      - 'PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin'
  before_script:
    - terraform init -backend=true -get=true -input=false

.job_grafana_user_creation: &grafana_user_creation_template
  tags:
    - docker
    - dind
  variables:
    AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
    AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
    AWS_DEFAULT_REGION: $AWS_DEFAULT_REGION
  script:
    - export TF_VAR_ENVIRONMENT=automation-${ENVIRONMENT_SLUG}
    - apk --upgrade --no-cache add curl python3 bash py-pip
    - pip3 install requests
    - curl -o /usr/local/bin/ecs-cli https://amazon-ecs-cli.s3.amazonaws.com/ecs-cli-linux-amd64-latest
    - chmod +x /usr/local/bin/ecs-cli
    - /bin/bash ci-utils/task_healthcheck.sh -t grafana
    - python3 ci-utils/grafana_users_creation.py

# ===============
# Modules linters
# ===============

igzpackages-linter:
  variables:
    MODULE: custompackages/igzpackages/
  <<: *lint_module

velocloud-linter:
  variables:
    MODULE: custompackages/velocloud/velocloud_client
  <<: *lint_module

notifier-linter:
  variables:
    MODULE: notifier
  <<: *lint_module

velocloud-orchestrator-linter:
  variables:
    MODULE: velocloud-orchestrator
  <<: *lint_module

last-contact-report-linter:
  variables:
    MODULE: last-contact-report
  <<: *lint_module

service-outage-triage-linter:
  variables:
    MODULE: service-outage-triage
  <<: *lint_module

service-outage-monitor-linter:
  variables:
    MODULE: service-outage-monitor
  <<: *lint_module

service-affecting-monitor-linter:
  variables:
    MODULE: service-affecting-monitor
  <<: *lint_module

velocloud-bridge-linter:
  variables:
    MODULE: velocloud-bridge
  <<: *lint_module

bruin-bridge:
  variables:
    MODULE: bruin-bridge
  <<: *lint_module

acceptance-test-linter:
  variables:
    MODULE: acceptance-tests
  <<: *lint_module

t7-bridge-linter:
  variables:
    MODULE: t7-bridge
  <<: *lint_module

#
# Modules unit tests
#
igzpackages-test:
  variables:
    MODULE: custompackages/igzpackages
  stage: unit_test
  image: python:3.6
  before_script:
    - apt-get update -q -y
    - cd ${MODULE}
    - python setup.py install
  script:
    - pytest --cov

velocloud-test:
  variables:
    MODULE: velocloud_client
  stage: unit_test
  image: python:3.6
  before_script:
    - apt-get update -q -y
    - cd custompackages/velocloud/
    - python setup.py install
    - cd ${MODULE}
  script:
    - pytest --cov

notifier-test:
  variables:
    MODULE: notifier
  <<: *unittest_module

velocloud-orchestrator-test:
  variables:
    MODULE: velocloud-orchestrator
  <<: *unittest_module

last-contact-report-test:
  variables:
    MODULE: last-contact-report
  <<: *unittest_module


service-outage-triage-test:
  variables:
    MODULE: service-outage-triage
  <<: *unittest_module

service-outage-monitor-test:
  variables:
    MODULE: service-outage-monitor
  <<: *unittest_module

service-affecting-monitor-test:
  variables:
    MODULE: service-affecting-monitor
  <<: *unittest_module

velocloud-bridge-test:
  variables:
    MODULE: velocloud-bridge
  <<: *unittest_module

bruin-bridge-test:
  variables:
    MODULE: bruin-bridge
  <<: *unittest_module

t7-bridge-test:
  variables:
    MODULE: t7-bridge
  <<: *unittest_module

#
# Acceptance/Integration tests
#
event_sourcing_acceptance_test:
  variables:
    BLUEPRINT: ${HONESTCODE_EVENT_SOURCING_BLUEPRINT}
  <<: *acceptance_test_module

#
# Images builds
#
nats-streaming-build:
  stage: build
  variables:
    MODULE_NAME: nats-streaming-server
    MODULE_VERSION: ${NATS_MODULE_VERSION}
  <<: *build_app_image

grafana-build:
  stage: build
  variables:
    MODULE_NAME: metrics-dashboard/grafana
    MODULE_VERSION: ${CI_PIPELINE_ID}
  services:
    - docker:18-dind
  before_script:
    - apk add --no-cache curl jq python py-pip bash
    - pip install awscli
    - $(aws ecr get-login --no-include-email --region us-east-1)
  script:
    - source ci-utils/environment_assign.sh
    - sed -i "s/ENVIRONMENT/${ENVIRONMENT_VAR}/g" metrics-dashboard/grafana/configs/prometheus-dev.yml
    - cp metrics-dashboard/grafana/configs/prometheus-dev.yml metrics-dashboard/grafana/datasources/prometheus.yml
    - docker build -t ${REPOSITORY_URL}-${MODULE_NAME}:latest -t ${REPOSITORY_URL}-${MODULE_NAME}:${CI_PIPELINE_ID} -f ${MODULE_NAME}/Dockerfile .
    - docker push ${REPOSITORY_URL}-${MODULE_NAME}:latest
    - docker push ${REPOSITORY_URL}-${MODULE_NAME}:${CI_PIPELINE_ID}
  after_script:
    - docker rmi ${REPOSITORY_URL}-${MODULE_NAME}:latest
    - docker rmi ${REPOSITORY_URL}-${MODULE_NAME}:${CI_PIPELINE_ID}

prometheus-build:
  stage: build
  variables:
    MODULE_NAME: metrics-dashboard/prometheus
    MODULE_VERSION: ${CI_PIPELINE_ID}
  services:
    - docker:18-dind
  before_script:
    - apk add --no-cache curl jq python py-pip bash
    - pip install awscli
    - $(aws ecr get-login --no-include-email --region us-east-1)
  script:
    - source ci-utils/environment_assign.sh
    - sed -i "s/ENVIRONMENT/${ENVIRONMENT_VAR}/g" metrics-dashboard/prometheus/configs/prometheus-dev.yml
    - cp metrics-dashboard/prometheus/configs/prometheus-dev.yml metrics-dashboard/prometheus/prometheus.yml
    - docker build -t ${REPOSITORY_URL}-${MODULE_NAME}:latest -t ${REPOSITORY_URL}-${MODULE_NAME}:${CI_PIPELINE_ID} -f ${MODULE_NAME}/Dockerfile .
    - docker push ${REPOSITORY_URL}-${MODULE_NAME}:latest
    - docker push ${REPOSITORY_URL}-${MODULE_NAME}:${CI_PIPELINE_ID}
  after_script:
    - docker rmi ${REPOSITORY_URL}-${MODULE_NAME}:latest
    - docker rmi ${REPOSITORY_URL}-${MODULE_NAME}:${CI_PIPELINE_ID}



notifier-build:
  stage: build
  variables:
    MODULE_NAME: notifier
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

velocloud-bridge-build:
  stage: build
  variables:
    MODULE_NAME: velocloud-bridge
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

bruin-bridge-build:
  stage: build
  variables:
    MODULE_NAME: bruin-bridge
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

velocloud-orchestrator-build:
  stage: build
  variables:
    MODULE_NAME: velocloud-orchestrator
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

last-contact-report-build:
  stage: build
  variables:
    MODULE_NAME: last-contact-report
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

service-outage-triage-build:
  stage: build
  variables:
    MODULE_NAME: service-outage-triage
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

service-outage-monitor-build:
  stage: build
  variables:
    MODULE_NAME: service-outage-monitor
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

service-affecting-monitor-build:
  stage: build
  variables:
    MODULE_NAME: service-affecting-monitor
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

t7-bridge-build:
  stage: build
  variables:
    MODULE_NAME: t7-bridge
    MODULE_VERSION: ${CI_PIPELINE_ID}
  <<: *build_app_image

# =========
# TERRAFORM
# =========

terraform-validate-basic-infra:
  stage: validation
  <<: *terraform_template
  script:
    - terraform init infra-as-code/basic-infra
    - terraform validate infra-as-code/basic-infra

terraform-validate-network-resources:
  stage: validation
  <<: *terraform_template
  script:
    - export TF_VAR_CURRENT_ENVIRONMENT="dev"
    - envsubst < infra-as-code/network-resources/init.tf > temp_network_resources_init.tf
    - mv temp_network_resources_init.tf infra-as-code/network-resources/init.tf
    - terraform init infra-as-code/network-resources
    - terraform validate infra-as-code/network-resources

terraform-validate-dev:
  stage: validation
  <<: *terraform_template
  script:
    - export ENVIRONMENT_SLUG=$(echo -n "${CI_COMMIT_REF_SLUG}" | sha256sum | cut -c1-8)
    - export TF_VAR_ENVIRONMENT=automation-${ENVIRONMENT_SLUG}
    - export TF_VAR_BUILD_NUMBER=${CI_PIPELINE_ID}
    - export TF_VAR_VELOCLOUD_CREDENTIALS=${VELOCLOUD_CREDENTIALS_DEV}
    - export TF_VAR_EMAIL_ACC_PWD=${EMAIL_ACC_PWD}
    - export TF_VAR_MONITORING_SECONDS=${MONITORING_SECONDS}
    - export TF_VAR_LAST_CONTACT_RECIPIENT=${LAST_CONTACT_RECIPIENT_DEV}
    - export TF_VAR_BRUIN_CLIENT_ID_PRO=${BRUIN_CLIENT_ID_PRO}
    - export TF_VAR_BRUIN_CLIENT_SECRET_PRO=${BRUIN_CLIENT_SECRET_PRO}
    - export TF_VAR_BRUIN_LOGIN_URL_PRO=${BRUIN_LOGIN_URL_PRO}
    - export TF_VAR_BRUIN_BASE_URL_PRO=${BRUIN_BASE_URL_PRO}
    - export TF_VAR_CURRENT_ENVIRONMENT=dev
    - apk add gettext
    - envsubst < infra-as-code/dev/init.tf > temp.tf
    - envsubst < infra-as-code/dev/variables.tf > temp_vars.tf
    - envsubst < infra-as-code/dev/references.tf > temp_references_dev.tf
    - mv temp.tf infra-as-code/dev/init.tf
    - mv temp_vars.tf infra-as-code/dev/variables.tf
    - mv temp_references_dev.tf infra-as-code/dev/references.tf
    - terraform init infra-as-code/dev
    - terraform validate infra-as-code/dev
    - envsubst < infra-as-code/ecs-services/init.tf > temp_ecs_services.tf
    - envsubst < infra-as-code/ecs-services/variables.tf > temp_ecs_services_vars.tf
    - envsubst < infra-as-code/ecs-services/references.tf > temp_references_ecs_services.tf
    - mv temp_ecs_services.tf infra-as-code/ecs-services/init.tf
    - mv temp_ecs_services_vars.tf infra-as-code/ecs-services/variables.tf
    - mv temp_references_ecs_services.tf infra-as-code/ecs-services/references.tf
    - terraform init infra-as-code/ecs-services
    - terraform validate infra-as-code/ecs-services

basic-infra:
  stage: basic_infra
  <<: *terraform_template
  script:
    - terraform init infra-as-code/basic-infra
    - terraform apply -auto-approve infra-as-code/basic-infra

network-resources-infra-branches:
  stage: basic_infra
  <<: *terraform_template
  script:
    - export TF_VAR_CURRENT_ENVIRONMENT="dev"
    - envsubst < infra-as-code/network-resources/init.tf > temp_network_resources_init.tf
    - mv temp_network_resources_init.tf infra-as-code/network-resources/init.tf
    - terraform init infra-as-code/network-resources
    - terraform validate infra-as-code/network-resources
    - terraform apply -auto-approve infra-as-code/network-resources
  only:
    - branches
  except:
    - master

network-resources-infra-master:
  stage: basic_infra
  <<: *terraform_template
  script:
    - export TF_VAR_CURRENT_ENVIRONMENT="production"
    - envsubst < infra-as-code/network-resources/init.tf > temp_network_resources_init.tf
    - mv temp_network_resources_init.tf infra-as-code/network-resources/init.tf
    - terraform init infra-as-code/network-resources
    - terraform validate infra-as-code/network-resources
    - terraform apply -auto-approve infra-as-code/network-resources
  only:
    - master

deploy-branches:
  stage: deploy
  <<: *terraform_template
  variables:
    AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
    AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
    AWS_DEFAULT_REGION: $AWS_DEFAULT_REGION
  script:
    - export ENVIRONMENT_SLUG=$(echo -n "${CI_COMMIT_REF_SLUG}" | sha256sum | cut -c1-8)
    - export TF_VAR_ENVIRONMENT=automation-${ENVIRONMENT_SLUG}
    - export TF_VAR_SUBDOMAIN=${ENVIRONMENT_SLUG}
    - export TF_VAR_BUILD_NUMBER=${CI_PIPELINE_ID}
    - export TF_VAR_NATS_MODULE_VERSION=${NATS_MODULE_VERSION}
    - export TF_VAR_VELOCLOUD_CREDENTIALS=${VELOCLOUD_CREDENTIALS_DEV}
    ##TODO: Restore to previous value assigning PRO environment values
    #- export TF_VAR_VELOCLOUD_CREDENTIALS=${VELOCLOUD_CREDENTIALS_PRO}
    - export TF_VAR_EMAIL_ACC_PWD=${EMAIL_ACC_PWD}
    - export TF_VAR_MONITORING_SECONDS=${MONITORING_SECONDS}
    - export TF_VAR_LAST_CONTACT_RECIPIENT=${LAST_CONTACT_RECIPIENT_DEV}
    - export TF_VAR_BRUIN_CLIENT_ID=${BRUIN_CLIENT_ID_PRO}
    - export TF_VAR_BRUIN_CLIENT_SECRET=${BRUIN_CLIENT_SECRET_PRO}
    - export TF_VAR_BRUIN_LOGIN_URL=${BRUIN_LOGIN_URL_PRO}
    - export TF_VAR_BRUIN_BASE_URL=${BRUIN_BASE_URL_PRO}
    - export TF_VAR_T7_BASE_URL=${T7_BASE_URL_DEV}
    - export TF_VAR_T7_TOKEN=${T7_TOKEN_DEV}
    - export TF_VAR_CURRENT_ENVIRONMENT=dev
    - apk add gettext
    - apk --upgrade --no-cache add curl
    - apk add bash
    - curl -o /usr/local/bin/ecs-cli https://amazon-ecs-cli.s3.amazonaws.com/ecs-cli-linux-amd64-latest
    - chmod +x /usr/local/bin/ecs-cli
    - envsubst < infra-as-code/dev/init.tf > temp.tf
    - envsubst < infra-as-code/dev/variables.tf > temp_vars.tf
    - mv temp.tf infra-as-code/dev/init.tf
    - mv temp_vars.tf infra-as-code/dev/variables.tf
    - terraform init infra-as-code/dev
    - terraform apply -auto-approve infra-as-code/dev
    - /bin/bash ci-utils/task_healthcheck.sh -t nats
    - envsubst < infra-as-code/ecs-services/init.tf > temp_ecs_services.tf
    - envsubst < infra-as-code/ecs-services/variables.tf > temp_ecs_services_vars.tf
    - mv temp_ecs_services.tf infra-as-code/ecs-services/init.tf
    - mv temp_ecs_services_vars.tf infra-as-code/ecs-services/variables.tf
    - terraform init infra-as-code/ecs-services
    - terraform apply -auto-approve infra-as-code/ecs-services
  environment:
    name: $CI_COMMIT_REF_NAME
    url: http://${ENVIRONMENT_SLUG}.mettel-automation.net/
    on_stop: destroy-branches
  only:
    - branches
  except:
    - master

destroy-branches:
  stage: deploy
  <<: *terraform_template
  script:
    - export ENVIRONMENT_SLUG=$(echo -n "${CI_COMMIT_REF_SLUG}" | sha256sum | cut -c1-8)
    - export TF_VAR_ENVIRONMENT=automation-${ENVIRONMENT_SLUG}
    - export TF_VAR_SUBDOMAIN=${ENVIRONMENT_SLUG}
    - apk add gettext
    - envsubst < infra-as-code/ecs-services/init.tf > temp_ecs_services.tf
    - envsubst < infra-as-code/ecs-services/variables.tf > temp_ecs_services_vars.tf
    - mv temp_ecs_services.tf infra-as-code/ecs-services/init.tf
    - mv temp_ecs_services_vars.tf infra-as-code/ecs-services/variables.tf
    - terraform init infra-as-code/ecs-services
    - terraform refresh infra-as-code/ecs-services
    - terraform destroy -auto-approve infra-as-code/ecs-services
    - envsubst < infra-as-code/dev/init.tf > temp.tf
    - envsubst < infra-as-code/dev/variables.tf > temp_vars.tf
    - mv temp.tf infra-as-code/dev/init.tf
    - mv temp_vars.tf infra-as-code/dev/variables.tf
    - terraform init infra-as-code/dev
    - terraform refresh infra-as-code/dev
    - terraform destroy -auto-approve infra-as-code/dev
  environment:
    name: $CI_COMMIT_REF_NAME
    action: stop
  when: manual
  only:
    - branches
  except:
    - master

deploy-master:
  stage: deploy
  <<: *terraform_template
  variables:
    AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
    AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
    AWS_DEFAULT_REGION: $AWS_DEFAULT_REGION
  script:
    - export ENVIRONMENT_SLUG=master
    - export TF_VAR_ENVIRONMENT=automation-${ENVIRONMENT_SLUG}
    - export TF_VAR_SUBDOMAIN=${ENVIRONMENT_SLUG}
    - export TF_VAR_BUILD_NUMBER=${CI_PIPELINE_ID}
    - export TF_VAR_NATS_MODULE_VERSION=${NATS_MODULE_VERSION}
    - export TF_VAR_VELOCLOUD_CREDENTIALS=${VELOCLOUD_CREDENTIALS_PRO}
    - export TF_VAR_EMAIL_ACC_PWD=${EMAIL_ACC_PWD}
    - export TF_VAR_MONITORING_SECONDS=${MONITORING_SECONDS}
    - export TF_VAR_LAST_CONTACT_RECIPIENT=${LAST_CONTACT_RECIPIENT_PRO}
    - export TF_VAR_BRUIN_CLIENT_ID=${BRUIN_CLIENT_ID_PRO}
    - export TF_VAR_BRUIN_CLIENT_SECRET=${BRUIN_CLIENT_SECRET_PRO}
    - export TF_VAR_BRUIN_LOGIN_URL=${BRUIN_LOGIN_URL_PRO}
    - export TF_VAR_BRUIN_BASE_URL=${BRUIN_BASE_URL_PRO}
    - export TF_VAR_T7_BASE_URL=${T7_BASE_URL_PRO}
    - export TF_VAR_T7_TOKEN=${T7_TOKEN_PRO}
    - export TF_VAR_CURRENT_ENVIRONMENT=production
    - apk add gettext
    - apk --upgrade --no-cache add curl
    - apk add bash
    - curl -o /usr/local/bin/ecs-cli https://amazon-ecs-cli.s3.amazonaws.com/ecs-cli-linux-amd64-latest
    - chmod +x /usr/local/bin/ecs-cli
    - envsubst < infra-as-code/dev/init.tf > temp.tf
    - envsubst < infra-as-code/dev/variables.tf > temp_vars.tf
    - mv temp.tf infra-as-code/dev/init.tf
    - mv temp_vars.tf infra-as-code/dev/variables.tf
    - terraform init infra-as-code/dev
    - terraform apply -auto-approve infra-as-code/dev
    - /bin/bash ci-utils/task_healthcheck.sh -t nats
    - envsubst < infra-as-code/ecs-services/init.tf > temp_ecs_services.tf
    - envsubst < infra-as-code/ecs-services/variables.tf > temp_ecs_services_vars.tf
    - mv temp_ecs_services.tf infra-as-code/ecs-services/init.tf
    - mv temp_ecs_services_vars.tf infra-as-code/ecs-services/variables.tf
    - terraform init infra-as-code/ecs-services
    - terraform apply -auto-approve infra-as-code/ecs-services
  environment:
    name: $CI_COMMIT_REF_NAME
    url: http://${ENVIRONMENT_SLUG}.mettel-automation.net:3000/
    on_stop: destroy-master
  only:
    - master

destroy-master:
  stage: deploy
  <<: *terraform_template
  script:
    - export ENVIRONMENT_SLUG=master
    - export TF_VAR_ENVIRONMENT=automation-${ENVIRONMENT_SLUG}
    - export TF_VAR_SUBDOMAIN=${ENVIRONMENT_SLUG}
    - apk add gettext
    - envsubst < infra-as-code/ecs-services/init.tf > temp_ecs_services.tf
    - envsubst < infra-as-code/ecs-services/variables.tf > temp_ecs_services_vars.tf
    - mv temp_ecs_services.tf infra-as-code/ecs-services/init.tf
    - mv temp_ecs_services_vars.tf infra-as-code/ecs-services/variables.tf
    - terraform init infra-as-code/ecs-services
    - terraform destroy -auto-approve infra-as-code/ecs-services
    - envsubst < infra-as-code/dev/init.tf > temp.tf
    - envsubst < infra-as-code/dev/variables.tf > temp_vars.tf
    - mv temp.tf infra-as-code/dev/init.tf
    - mv temp_vars.tf infra-as-code/dev/variables.tf
    - terraform init infra-as-code/dev
    - terraform destroy -auto-approve infra-as-code/dev
  environment:
    name: $CI_COMMIT_REF_NAME
    action: stop
  when: manual
  only:
    - master

destroy-network-resources-branches:
  stage: deploy
  <<: *terraform_template
  script:
    - export TF_VAR_CURRENT_ENVIRONMENT="dev"
    - envsubst < infra-as-code/network-resources/init.tf > temp_network_resources_init.tf
    - mv temp_network_resources_init.tf infra-as-code/network-resources/init.tf
    - terraform init infra-as-code/network-resources
    - terraform refresh infra-as-code/network-resources
    - terraform destroy -auto-approve infra-as-code/network-resources
  when: manual
  only:
    - branches
  except:
    - master

destroy-network-resources-master:
  stage: deploy
  <<: *terraform_template
  script:
    - export TF_VAR_CURRENT_ENVIRONMENT="production"
    - envsubst < infra-as-code/network-resources/init.tf > temp_network_resources_init.tf
    - mv temp_network_resources_init.tf infra-as-code/network-resources/init.tf
    - terraform init infra-as-code/network-resources
    - terraform refresh infra-as-code/network-resources
    - terraform destroy -auto-approve infra-as-code/network-resources
  when: manual
  only:
    - master

grafana_user_creation_branches:
  stage: grafana_user_creation
  <<: *grafana_user_creation_template
  before_script:
    - export ENVIRONMENT_SLUG=$(echo -n "${CI_COMMIT_REF_SLUG}" | sha256sum | cut -c1-8)
  only:
    - branches
  except:
    - master

grafana_user_creation_master:
  stage: grafana_user_creation
  <<: *grafana_user_creation_template
  before_script:
    - export ENVIRONMENT_SLUG=master
  only:
    - master